# 🚀 Инструкция по установке на сервер

## Быстрая установка

### Шаг 1: Подключение к серверу

**Через SSH:**
```bash
ssh root@45.15.126.22
# Пароль: 42O4UZ8HWPBe
```

**Или через панель VMmanager:**
- Зайдите на https://jarvis.panel.services
- Войдите: naggasalen@gmail.com / VXvo6ROWYCrR
- Используйте консоль в панели

### Шаг 2: Загрузка скриптов на сервер

**Вариант А: Через SCP (с вашего ПК)**
```bash
# В PowerShell на вашем ПК
scp deploy.sh root@45.15.126.22:/root/
scp setup_nginx.sh root@45.15.126.22:/root/
```

**Вариант Б: Создать файлы прямо на сервере**
```bash
# На сервере
nano deploy.sh
# Скопируйте содержимое файла deploy.sh
# Сохраните: Ctrl+O, Enter, Ctrl+X

nano setup_nginx.sh
# Скопируйте содержимое файла setup_nginx.sh
```

### Шаг 3: Запуск установки

```bash
# Сделать скрипты исполняемыми
chmod +x deploy.sh setup_nginx.sh

# Запустить установку
bash deploy.sh
```

Скрипт автоматически:
- ✅ Обновит систему
- ✅ Установит Node.js 20.x
- ✅ Установит PM2
- ✅ Загрузит проект из GitHub
- ✅ Установит зависимости
- ✅ Запустит приложение
- ✅ Настроит автозапуск

### Шаг 4: Настройка Nginx (для работы через домен)

```bash
bash setup_nginx.sh
```

### Шаг 5: Настройка DNS

В настройках домена `whitepage.com` добавьте:
- **A запись:** `whitepage.com` → `45.15.126.22`
- **A запись:** `www.whitepage.com` → `45.15.126.22`

## Доступ к приложению

После установки приложение будет доступно:
- `http://45.15.126.22:3000` (прямой доступ)
- `http://whitepage.com` (через домен, после настройки DNS)

## Управление приложением

```bash
# Статус
pm2 status

# Логи
pm2 logs checklist

# Перезапуск
pm2 restart checklist

# Остановка
pm2 stop checklist

# Перезагрузка после изменений в коде
cd /var/www/checklist
git pull
npm install
pm2 restart checklist
```

## Загрузка сайтов на сервер

Для проверки сайтов их нужно загрузить на сервер:

**Через SCP:**
```bash
# С вашего ПК
scp -r "C:\Users\840G5\Desktop\sites" root@45.15.126.22:/var/www/sites
```

**Через FTP:**
- Используйте FileZilla или другой FTP клиент
- Подключитесь к `45.15.126.22`
- Загрузите папки с сайтами в `/var/www/sites`

**Через Git:**
- Создайте репозиторий с сайтами
- Клонируйте на сервер

## Безопасность

⚠️ **ВАЖНО:** После первого входа смените пароль root:
```bash
passwd
```

## Проверка работы

1. Откройте `http://45.15.126.22:3000` в браузере
2. Укажите путь к сайтам на сервере (например: `/var/www/sites`)
3. Запустите анализ

## Устранение проблем

**Порт занят:**
```bash
# Проверка
netstat -tulpn | grep 3000
# Изменить порт в server.js
```

**PM2 не запускается:**
```bash
pm2 logs checklist
# Проверьте ошибки в логах
```

**Nginx не работает:**
```bash
nginx -t
systemctl status nginx
tail -f /var/log/nginx/checklist_error.log
```

