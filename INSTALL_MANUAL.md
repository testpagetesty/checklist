# 📋 Ручная установка на сервер (без SCP)

## Способ 1: Создать файлы прямо на сервере (самый простой)

### Шаг 1: Подключитесь к серверу

**Через PuTTY (Windows):**
1. Скачайте PuTTY: https://www.putty.org/
2. Запустите PuTTY
3. Host Name: `45.15.126.22`
4. Port: `22`
5. Connection type: SSH
6. Нажмите Open
7. Логин: `root`
8. Пароль: `42O4UZ8HWPBe`

**Или через PowerShell:**
```powershell
ssh root@45.15.126.22
# Пароль: 42O4UZ8HWPBe
```

### Шаг 2: Создайте файл deploy.sh на сервере

На сервере выполните:
```bash
nano deploy.sh
```

Скопируйте весь текст из файла `deploy.sh` (откройте его на вашем ПК) и вставьте в nano.
- Сохраните: `Ctrl+O`, затем `Enter`
- Выйдите: `Ctrl+X`

### Шаг 3: Создайте файл setup_nginx.sh

```bash
nano setup_nginx.sh
```

Скопируйте весь текст из файла `setup_nginx.sh` и вставьте.

### Шаг 4: Запустите установку

```bash
chmod +x deploy.sh setup_nginx.sh
bash deploy.sh
```

---

## Способ 2: Использовать WinSCP (графический интерфейс)

1. Скачайте WinSCP: https://winscp.net/
2. Установите и запустите
3. Создайте новое подключение:
   - Host name: `45.15.126.22`
   - Port: `22`
   - Username: `root`
   - Password: `42O4UZ8HWPBe`
   - Protocol: SFTP
4. Подключитесь
5. Перетащите файлы `deploy.sh` и `setup_nginx.sh` в папку `/root/`
6. Закройте WinSCP
7. Подключитесь через SSH (PuTTY) и запустите:
   ```bash
   chmod +x deploy.sh setup_nginx.sh
   bash deploy.sh
   ```

---

## Способ 3: Использовать FileZilla

1. Скачайте FileZilla: https://filezilla-project.org/
2. Файл → Менеджер сайтов → Новый сайт
3. Заполните:
   - Хост: `sftp://45.15.126.22`
   - Порт: `22`
   - Протокол: SFTP
   - Тип входа: Обычный
   - Пользователь: `root`
   - Пароль: `42O4UZ8HWPBe`
4. Подключитесь
5. Загрузите файлы `deploy.sh` и `setup_nginx.sh` в `/root/`
6. Подключитесь через SSH и запустите установку

---

## Способ 4: Копировать команды напрямую (самый быстрый)

Просто выполните команды на сервере:

```bash
# Подключитесь к серверу
ssh root@45.15.126.22

# Обновление системы
apt update && apt upgrade -y

# Установка Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs git build-essential

# Установка PM2
npm install -g pm2

# Создание директории и клонирование проекта
mkdir -p /var/www
cd /var/www
git clone https://github.com/testpagetesty/checklist.git
cd checklist

# Установка зависимостей
npm install

# Запуск через PM2
pm2 start server.js --name checklist
pm2 save
pm2 startup

# Открытие порта
ufw allow 3000/tcp || echo "Проверьте файрвол вручную"

# Проверка
pm2 status
```

После этого приложение будет доступно по адресу: `http://45.15.126.22:3000`

---

## Рекомендация

**Самый простой способ:** Используйте Способ 4 - просто скопируйте команды и выполните их на сервере через SSH. Это быстрее всего!

