# 📖 Инструкция по использованию системы анализа сайтов

## 🚀 Быстрый старт

### Вариант 1: Автоматический запуск (рекомендуется)

1. **Дважды кликните на файл `START.bat`**
   - Скрипт автоматически проверит Node.js
   - Установит зависимости (если нужно)
   - Запустит веб-сервер

2. **Откройте браузер** и перейдите по адресу:
   ```
   http://localhost:3000
   ```

3. **Нажмите кнопку "🚀 Запустить анализ"**
   - Анализ запустится автоматически
   - Результаты отобразятся в интерфейсе
   - Отчет будет доступен сразу после завершения

### Вариант 2: Ручной запуск

1. Откройте командную строку в папке `дизайнер25`

2. Установите зависимости (если еще не установлены):
   ```bash
   npm install
   ```

3. Запустите сервер:
   ```bash
   npm start
   ```
   или
   ```bash
   node server.js
   ```

4. Откройте браузер: `http://localhost:3000`

## 📋 Как использовать интерфейс

### 1. Выбор папки для анализа

- **Текущая папка**: Оставьте поле "Папка для анализа" пустым - будет проанализирована папка `дизайнер25`
- **Другая папка**: Введите полный путь к папке с сайтами (например: `C:\Users\Имя\Проекты\Сайты`)

### 2. Запуск анализа

1. Нажмите кнопку **"🚀 Запустить анализ"**
2. Дождитесь завершения (прогресс отображается в реальном времени)
3. После завершения появится статистика и кнопка просмотра отчета

### 3. Просмотр отчета

- Нажмите **"📊 Просмотреть отчет"** для просмотра детального HTML отчета
- Отчет автоматически откроется после завершения анализа

### 4. Обновление списка

- Кнопка **"🔄 Обновить список"** показывает количество найденных сайтов

## 📊 Что проверяется

Система автоматически проверяет:

- ✅ Существование папки сайта
- ✅ Наличие главной страницы (light.html, index.html, home.html)
- ✅ Наличие страницы контактов (iletisim.html, contact.html)
- ✅ Количество документов (Privacy Policy, Terms и т.д.)
- ✅ Количество изображений в папках images/image/img
- ✅ Наличие CSS файлов/папок
- ✅ Наличие JS файлов/папок

## 📁 Структура файлов

```
дизайнер25/
├── START.bat              # Быстрый запуск
├── server.js              # Сервер Node.js
├── index.html             # Веб-интерфейс
├── check_sites.ps1        # PowerShell скрипт проверки
├── structure_report.html  # Генерируемый отчет
└── package.json           # Зависимости проекта
```

## ⚠️ Требования

- **Node.js** версии 14 или выше
- **Браузер** (Chrome, Firefox, Edge)

> **Примечание:** Система теперь использует Node.js вместо PowerShell, поэтому работает на любой платформе (Windows, Mac, Linux)

## 🔧 Устранение проблем

### Порт 3000 занят

Если порт 3000 занят, измените порт в файле `server.js`:
```javascript
const PORT = 3001; // или другой свободный порт
```

### Скрипт не запускается

Система теперь использует Node.js напрямую, PowerShell не требуется. Если возникают проблемы, проверьте версию Node.js:
```bash
node --version
```
Должна быть версия 14 или выше.

### Ошибка "Cannot find module 'express'"

Установите зависимости:
```bash
npm install
```

## 💡 Советы

- Отчет сохраняется в файл `structure_report.html` и может быть открыт отдельно
- Можно анализировать любую папку с сайтами, указав путь
- Интерфейс работает в реальном времени - видно прогресс выполнения

